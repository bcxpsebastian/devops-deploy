apiVersion: apps/v1
kind: Deployment
metadata:
  name: dogs
  namespace: dogs
  labels:
    app: dogs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dogs
  template:
    metadata:
      labels:
        app: dogs
    spec:
      containers:
        - name: dogs
          # Replace <GITHUB_OWNER> with your GitHub org/user if needed
          image: ghcr.io/bcxpsebastian/dogs:561c00c98196c9efbfefee8411959f0bff443e56
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
